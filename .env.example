# Required for the live Gemini extraction node.
GEMINI_API_KEY=
GEMINI_MODEL=gemini-3.5-flash-lite

# Required for uploaded Lesson 7 baseline experiments; otherwise optional.
# The baseline-live suite also requires the Gemini credentials above.
LANGSMITH_TRACING=true
LANGSMITH_API_KEY=
LANGSMITH_PROJECT=interview-copilot-workflow
